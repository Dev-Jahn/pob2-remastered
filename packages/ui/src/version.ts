/**
 * The @pob2/ui package version. Trivial value the scaffold test asserts to prove
 * the Vitest + jsdom runner is wired; kept in sync with package.json by hand for
 * now (no build-time injection yet).
 */
export const UI_VERSION = '0.0.0';
