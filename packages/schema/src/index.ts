/**
 * @pob2/schema — shared IPC and build-state types + JSON Schemas.
 *
 * Mirrors the Core API contract (DESIGN.md §6.3-§6.4) and BuildState (§12.1).
 * Implemented from Phase 1; this placeholder establishes the package's TS build
 * so `tsc -b` (pnpm typecheck) has a real composite project to check.
 */
export {};
